*.beam
*.ez
/build
erl_crash.dump
.DS_Store

# because I'm a silly JS guy that doesn't know other file watchers 
nodemon.json

# Local images for the optional private ExifTool parity test.
/test/private-fixtures/
